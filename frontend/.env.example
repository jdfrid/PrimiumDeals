# Leave empty for same-origin API (Render one service, or Vite dev proxy to /api).
# Only for a split deploy (static site + API elsewhere), set the backend origin ONCE at build time — no /api suffix:
# VITE_API_URL=https://your-service.onrender.com
